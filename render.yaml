services:
  - type: web
    name: whatsapp-bulksender
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
